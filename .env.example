# PostgreSQL connection string (required)
DATABASE_URL=postgresql://tupisec:changeme123@localhost:5432/tupisec

# Secret key for AES-256-GCM encryption of API keys and session tokens (required in production)
TUPISEC_SECRET=change-me-in-production

# Enable dashboard authentication (optional, default: disabled)
# TUPISEC_AUTH_ENABLED=true

# Public base URL for report/monitor links in notifications (optional)
# NEXT_PUBLIC_BASE_URL=https://tupisec.example.com
